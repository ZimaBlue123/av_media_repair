[project]
name = "module1-video-repair"
version = "0.1.0"
description = "Repair and remux video files (Windows-first)."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

